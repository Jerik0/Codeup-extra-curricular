"use strict";

//TODO write a function called findCircumference that will take in a diameter and return the circumference

const findCircumference = (d) => {
  return parseFloat((d * Math.PI).toFixed(2));
};

//TODO write a function that will return the

//TODO write a function called factorsOf that returns the factors of a trinomial when given a, b, and c and each of their powers in order. e.g. 5x^2 + 6x^2 + x would input as factorsOf(5,2,6,2,8).

const factorsOf = (a,powA,b,powB,c,powC) => {

};
